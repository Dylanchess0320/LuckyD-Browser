# ── Core Dependencies ────────────────────────────────────────────────
# Pinned 2026-09-13 (verified local install; upper bounds keep CI reproducible).
httpx==0.28.1
rich==13.9.4
pydantic==2.13.5
python-dotenv==1.2.2
typing-extensions>=4.9,<5.0
fastapi==0.139.2
# uvicorn serves the Cline bridge (`python cline_bridge.py`); httpx powers fastapi's TestClient.
uvicorn==0.51.0

# ── Deep Research swarm (native tool: tools/deep_research_tool.py) ────
# langgraph is required; google-genai only for Gemini grounding (DRS_PROVIDER=gemini).
# ddgs powers keyless DDG search; trafilatura/bs4/lxml power full-page reads.
langgraph==1.2.11
google-genai==2.17.0
ddgs==9.14.4
trafilatura==2.2.0
beautifulsoup4==4.15.0
lxml==6.1.1
tldextract==5.3.2
tenacity==9.1.4
pyyaml==6.0.3
click==8.4.2

# ── ONNX Embeddings (optional: pip install .[onnx]) ────────────────
# onnxruntime>=1.17,<2.0
# sentence-transformers>=2.5,<3.0

# ── Browser Tools (optional: pip install .[browser]) ────────────────
# playwright>=1.42,<2.0
# pillow>=10.0,<11.0
