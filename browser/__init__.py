"""LuckyD Browser â€” a full-featured Chromium-based web browser for Windows."""

__version__ = "2.5.9"

# Shown once after an update (first launch with a new version).
WHATS_NEW = (
    "Terminal fixed — every shell (Agent 1, Agent 2, PowerShell, CMD, "
    "and the nine Agent Mesh CLIs) now spawns reliably again."
)
