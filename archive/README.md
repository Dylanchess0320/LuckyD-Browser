# Archive — scratch & temp files

These files are leftover scratch artifacts from past one-off tasks. Nothing in
the project references them, so they are safe to delete whenever you want.

| File | What it was |
|------|-------------|
| `feed_adapter.txt` | Kotlin `FeedAdapter.kt` code dump from the external `movie-recommender` Android project |
| `_temp_rb.txt` | Temporary text snippet |
| `all_assets.txt`, `all_py.txt` | Empty file-listing dumps (0 bytes) |
| `$path` | 11-byte leftover containing the text `$content` |
| `C?temptcg_snapshot.json` | Temp TCG snapshot JSON (stray character in filename) |

Moved here during the folder organization on 2026-07-28.
